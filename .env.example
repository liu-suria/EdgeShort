# Configure these as encrypted Environment Variables in EdgeOne Makers.
# Never commit their real values.
ADMIN_PASSWORD=replace-with-a-long-unique-password
SESSION_SECRET=replace-with-at-least-32-random-characters

# Bind an EdgeOne KV namespace to the project using this exact variable name.
URLS_KV=bound-in-edgeone-console
